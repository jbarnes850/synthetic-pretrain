include: configs/generated/blog_base_8192.yaml

train:
  condition: finephrase_math_ntp
  output_dir: outputs/blog_finephrase_8192
  learning_rate: 0.0002

selection:
  mode: fixed
  fixed_choice: finephrase
  candidates:
    - finephrase
