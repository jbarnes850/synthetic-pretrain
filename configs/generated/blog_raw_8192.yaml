include: configs/generated/blog_base_8192.yaml

train:
  condition: raw_ntp
  output_dir: outputs/blog_raw_8192
  learning_rate: 0.0003

selection:
  mode: fixed
  fixed_choice: original
  candidates:
    - original
