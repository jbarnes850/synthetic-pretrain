include: configs/thinking_sft_base.yaml

# Token-budget control trained on raw chunks from the same SFT split.

train:
  condition: raw_chunk_ntp
  output_dir: outputs/thinking_sft_raw_control
  max_steps: 2624
  warmup_steps: 128
