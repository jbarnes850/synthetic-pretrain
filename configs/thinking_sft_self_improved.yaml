include: configs/thinking_sft_base.yaml

# Same Thinking Mid-training SFT objective as the base arm, initialized from the
# Self-Improving Pretraining continued-pretraining checkpoint.

train:
  init_from_checkpoint: outputs/self_improving_pretraining/final.pt
  output_dir: outputs/thinking_sft_self_improved
